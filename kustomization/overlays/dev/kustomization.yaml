apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tpl-phoenix-pg-k8s-tilt

resources:
  - ../../namespace
  - ../../bases/database
  - ../../bases/server
  - secrets

patches:
  - path: patches/mix_env.yaml

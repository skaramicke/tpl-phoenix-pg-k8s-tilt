apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: server-set
spec:
  template:
    spec:
      containers:
        - name: server
          env:
            - name: MIX_ENV
              value: dev_cluster
